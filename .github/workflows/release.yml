name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

defaults:
  shell: bash

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: ./.github/actions/install

      - run: yarn publish
        env:
          NPM_AUTH_TOKEN: ${{ secret.NPM_AUTH_TOKEN }}

